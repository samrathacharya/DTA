/* 
 * Generated by Overcast UTC 2022-02-08 10:01:19Z.
 * Changes to this class may cause incorrect behavior and will be lost if the scenario is re-activated.
 */
@istest
private class SalesDocHeaderStatusTest {
    @istest
    private static void testSalesDocHeaderStatus() {
        SalesDocHeaderStatus actual = new SalesDocHeaderStatus();
        system.assertNotEquals(null, actual.VBUK);
    }

    @istest
    private static void testVBUK() {
        SalesDocHeaderStatus.VBUK actual = new SalesDocHeaderStatus.VBUK('a','a','a');

        system.assertEquals('a', actual.VBELN);
        system.assertEquals('a', actual.FSSTK);
        system.assertEquals('a', actual.LSSTK);

        SalesDocHeaderStatus.VBUK actual0 = new SalesDocHeaderStatus.VBUK();
    }
    @istest
    private static void testrunQuery() {
        SalesDocHeaderStatus request = testSetup();
        overcast.Query query1 = new overcast.Query();
        query1.limit_x = 10;
        query1.offset = 10;
        
        SalesDocHeaderStatus response = request.runQuery(query1);
        system.assertEquals('a', response.VBUK[0].VBELN);
        string requestid = request.runQueryAsync(query1, null, null, new Continuation(10));
    }

    private static SalesDocHeaderStatus testSetup() {
        overcast.TestingUtilities.SetupTestEnvironment('SalesDocHeaderStatus');
        SalesDocHeaderStatus request = new SalesDocHeaderStatus();
        SalesDocHeaderStatus.VBUK value1 = new SalesDocHeaderStatus.VBUK();
        value1.VBELN = 'a';
        request.VBUK.add(value1);

        string s = request.serialize();
        request.testResponse = '{"output":' + s + '}';
        return request;
    }


    @istest
    private static void testSerialize() {
        SalesDocHeaderStatus input = new SalesDocHeaderStatus();
        SalesDocHeaderStatus.VBUK value1 = new SalesDocHeaderStatus.VBUK();
        value1.VBELN = 'a';
        input.VBUK.add(value1);
        SalesDocHeaderStatus actual = (SalesDocHeaderStatus)JSON.deserialize(input.serialize(), SalesDocHeaderStatus.class);
        system.assertEquals('a', actual.VBUK[0].VBELN);
    }

    @istest
    private static void testDeserialize() {
        SalesDocHeaderStatus output = new SalesDocHeaderStatus();
        SalesDocHeaderStatus.VBUK value1 = new SalesDocHeaderStatus.VBUK();
        value1.VBELN = 'a';
        output.VBUK.add(value1);

        string s = output.serialize();
        SalesDocHeaderStatus actual = SalesDocHeaderStatus.deserialize('{"output":' + s + '}');
        system.assertEquals('a', actual.VBUK[0].VBELN);
    }

    @istest
    private static void testDeserializeError() {
        try {
            SalesDocHeaderStatus actual = SalesDocHeaderStatus.deserialize('{"error":["A test error"]}');
            system.assert(false, 'No exception thrown');
        }
        catch (SalesDocHeaderStatus.SalesDocHeaderStatusException e) {
            system.assertEquals('A test error', e.getMessage());
        }
    }

    @istest
    private static void testGetterSetter() {
        SalesDocHeaderStatus output = new SalesDocHeaderStatus();
        output.set('SAMPLE1', new map<string, object>{
            'FIELD1' => 'VALUE1'
        });
        
        system.assertEquals(null, output.get('NOVAL'));
        
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));		
    }
    
    @istest
    private static void testLoadAsyncResponse() {
        SalesDocHeaderStatus output = new SalesDocHeaderStatus();
        output.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        system.assertEquals(null, output.get('NOVAL'));
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));

        overcast.Globals.IsOCF = true;
        SalesDocHeaderStatus output2 = new SalesDocHeaderStatus();
        output2.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        overcast.Globals.IsOCF = false;
        system.assertEquals(null, output2.get('NOVAL'));
        map<string, object> sample2 = (map<string, object>)output2.get('SAMPLE1');
        system.assertNotEquals(null, sample2);
        system.assertEquals('VALUE1', sample2.get('FIELD1'));
    }

    @istest
    private static void testGetMetadata() {
        SalesDocHeaderStatus output = new SalesDocHeaderStatus();
        map<string, overcast.TypeInfo> metadata = output.getMetadata();
        
        set<string> keys = metadata.keySet();
        system.assert(keys.contains('VBUK'));
        
        overcast.TypeInfo first = metadata.get(output.getTopTypeName());
        system.assertNotEquals(null, first);
        system.assertEquals('VBUK', first.name);
    }

}