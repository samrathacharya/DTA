/* 
 * Generated by Overcast UTC 2021-12-20 09:32:50Z.
 * Changes to this class may cause incorrect behavior and will be lost if the scenario is re-activated.
 */
@istest
private class ShippingRouteDescRTTest {
    @istest
    private static void testShippingRouteDescRT() {
        ShippingRouteDescRT actual = new ShippingRouteDescRT();
        system.assertNotEquals(null, actual.TVROT);
    }

    @istest
    private static void testTVROT() {
        ShippingRouteDescRTTVROT actual = new ShippingRouteDescRTTVROT();

        actual.MANDT = 'a';
        actual.SPRAS = 'a';
        actual.ROUTE = 'a';
        actual.BEZEI = 'a';
        system.assertEquals('a', actual.MANDT);
        system.assertEquals('a', actual.SPRAS);
        system.assertEquals('a', actual.ROUTE);
        system.assertEquals('a', actual.BEZEI);
    }
    @istest
    private static void testrunQuery() {
        ShippingRouteDescRT request = testSetup();
        overcast.Query query1 = new overcast.Query();
        query1.limit_x = 10;
        query1.offset = 10;
        
        ShippingRouteDescRT response = request.runQuery(query1);
        system.assertEquals('a', response.TVROT[0].MANDT);
        string requestid = request.runQueryAsync(query1, null, null, new Continuation(10));
    }

    private static ShippingRouteDescRT testSetup() {
        overcast.TestingUtilities.SetupTestEnvironment('ShippingRouteDescRT');
        ShippingRouteDescRT request = new ShippingRouteDescRT();
        ShippingRouteDescRTTVROT value1 = new ShippingRouteDescRTTVROT();
        value1.MANDT = 'a';
        request.TVROT.add(value1);

        string s = request.serialize();
        request.testResponse = '{"output":' + s + '}';
        return request;
    }


    @istest
    private static void testSerialize() {
        ShippingRouteDescRT input = new ShippingRouteDescRT();
        ShippingRouteDescRTTVROT value1 = new ShippingRouteDescRTTVROT();
        value1.MANDT = 'a';
        input.TVROT.add(value1);
        ShippingRouteDescRT actual = (ShippingRouteDescRT)JSON.deserialize(input.serialize(), ShippingRouteDescRT.class);
        system.assertEquals('a', actual.TVROT[0].MANDT);
    }

    @istest
    private static void testDeserialize() {
        ShippingRouteDescRT output = new ShippingRouteDescRT();
        ShippingRouteDescRTTVROT value1 = new ShippingRouteDescRTTVROT();
        value1.MANDT = 'a';
        output.TVROT.add(value1);

        string s = output.serialize();
        ShippingRouteDescRT actual = ShippingRouteDescRT.deserialize('{"output":' + s + '}');
        system.assertEquals('a', actual.TVROT[0].MANDT);
    }

    @istest
    private static void testDeserializeError() {
        try {
            ShippingRouteDescRT actual = ShippingRouteDescRT.deserialize('{"error":["A test error"]}');
            system.assert(false, 'No exception thrown');
        }
        catch (ShippingRouteDescRT.ShippingRouteDescRTException e) {
            system.assertEquals('A test error', e.getMessage());
        }
    }

    @istest
    private static void testGetterSetter() {
        ShippingRouteDescRT output = new ShippingRouteDescRT();
        output.set('SAMPLE1', new map<string, object>{
            'FIELD1' => 'VALUE1'
        });
        
        system.assertEquals(null, output.get('NOVAL'));
        
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));		
    }
    
    @istest
    private static void testLoadAsyncResponse() {
        ShippingRouteDescRT output = new ShippingRouteDescRT();
        output.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        system.assertEquals(null, output.get('NOVAL'));
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));

        overcast.Globals.IsOCF = true;
        ShippingRouteDescRT output2 = new ShippingRouteDescRT();
        output2.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        overcast.Globals.IsOCF = false;
        system.assertEquals(null, output2.get('NOVAL'));
        map<string, object> sample2 = (map<string, object>)output2.get('SAMPLE1');
        system.assertNotEquals(null, sample2);
        system.assertEquals('VALUE1', sample2.get('FIELD1'));
    }

    @istest
    private static void testGetMetadata() {
        ShippingRouteDescRT output = new ShippingRouteDescRT();
        map<string, overcast.TypeInfo> metadata = output.getMetadata();
        
        set<string> keys = metadata.keySet();
        system.assert(keys.contains('TVROT'));
        
        overcast.TypeInfo first = metadata.get(output.getTopTypeName());
        system.assertNotEquals(null, first);
        system.assertEquals('TVROT', first.name);
    }

    
    @isTest
    static void testRunInvocableMethod() {
        overcast__Scenario__c scenario = overcast.TestingUtilities.setupTestEnvironment('ShippingRouteDescRT');
        overcast__Run__c run = new overcast__Run__c(overcast__Scenario__c = scenario.Id, overcast__Mode__c = 'Run', overcast__Operation__c = 'Read', overcast__Input__c = 'X', overcast__Manual__c = true);
        insert run;
        ShippingRouteDescRT.Parameters params = new ShippingRouteDescRT.Parameters();
        params.flowInterviewGuid = 'X';
        params.query = new overcast.Query();
        params.Logger = null;
        params.doNotCommitLogs = false;
        params.testResponse = '{"output": ' + JSON.serialize(new ShippingRouteDescRT()) + '}';
        List<ShippingRouteDescRT.Parameters> paramList = new List<ShippingRouteDescRT.Parameters>{ params };

        Test.startTest();
        List<ShippingRouteDescRT.Result> results = ShippingRouteDescRT.runInvocableMethod(paramList);
        Test.stopTest();

        System.assertEquals(1, results.size());
    }
}