/* 
 * Generated by Overcast UTC 2021-12-21 04:16:30Z.
 * Changes to this class may cause incorrect behavior and will be lost if the scenario is re-activated.
 */
@istest
private class MaterialPortalSiteTest {
    @istest
    private static void testMaterialPortalSite() {
        MaterialPortalSite actual = new MaterialPortalSite();
        system.assertNotEquals(null, actual.ZMM_PORTAL);
    }

    @istest
    private static void testZMM_PORTAL() {
        MaterialPortalSite.ZMM_PORTAL actual = new MaterialPortalSite.ZMM_PORTAL('a','a','a','a',Date.newInstance(2000,1,1),Date.newInstance(2000,1,1));

        system.assertEquals('a', actual.MANDT);
        system.assertEquals('a', actual.MATNR);
        system.assertEquals('a', actual.HYBRIS_SITE);
        system.assertEquals('a', actual.HYBRIS_STORE);
        system.assertEquals(Date.newInstance(2000,1,1), actual.DATAB);
        system.assertEquals(Date.newInstance(2000,1,1), actual.DATBI);

        MaterialPortalSite.ZMM_PORTAL actual0 = new MaterialPortalSite.ZMM_PORTAL();
    }
    @istest
    private static void testrunQuery() {
        MaterialPortalSite request = testSetup();
        overcast.Query query1 = new overcast.Query();
        query1.limit_x = 10;
        query1.offset = 10;
        
        MaterialPortalSite response = request.runQuery(query1);
        system.assertEquals('a', response.ZMM_PORTAL[0].MANDT);
        string requestid = request.runQueryAsync(query1, null, null, new Continuation(10));
    }

    private static MaterialPortalSite testSetup() {
        overcast.TestingUtilities.SetupTestEnvironment('MaterialPortalSite');
        MaterialPortalSite request = new MaterialPortalSite();
        MaterialPortalSite.ZMM_PORTAL value1 = new MaterialPortalSite.ZMM_PORTAL();
        value1.MANDT = 'a';
        request.ZMM_PORTAL.add(value1);

        string s = request.serialize();
        request.testResponse = '{"output":' + s + '}';
        return request;
    }


    @istest
    private static void testSerialize() {
        MaterialPortalSite input = new MaterialPortalSite();
        MaterialPortalSite.ZMM_PORTAL value1 = new MaterialPortalSite.ZMM_PORTAL();
        value1.MANDT = 'a';
        input.ZMM_PORTAL.add(value1);
        MaterialPortalSite actual = (MaterialPortalSite)JSON.deserialize(input.serialize(), MaterialPortalSite.class);
        system.assertEquals('a', actual.ZMM_PORTAL[0].MANDT);
    }

    @istest
    private static void testDeserialize() {
        MaterialPortalSite output = new MaterialPortalSite();
        MaterialPortalSite.ZMM_PORTAL value1 = new MaterialPortalSite.ZMM_PORTAL();
        value1.MANDT = 'a';
        output.ZMM_PORTAL.add(value1);

        string s = output.serialize();
        MaterialPortalSite actual = MaterialPortalSite.deserialize('{"output":' + s + '}');
        system.assertEquals('a', actual.ZMM_PORTAL[0].MANDT);
    }

    @istest
    private static void testDeserializeError() {
        try {
            MaterialPortalSite actual = MaterialPortalSite.deserialize('{"error":["A test error"]}');
            system.assert(false, 'No exception thrown');
        }
        catch (MaterialPortalSite.MaterialPortalSiteException e) {
            system.assertEquals('A test error', e.getMessage());
        }
    }

    @istest
    private static void testGetterSetter() {
        MaterialPortalSite output = new MaterialPortalSite();
        output.set('SAMPLE1', new map<string, object>{
            'FIELD1' => 'VALUE1'
        });
        
        system.assertEquals(null, output.get('NOVAL'));
        
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));		
    }
    
    @istest
    private static void testLoadAsyncResponse() {
        MaterialPortalSite output = new MaterialPortalSite();
        output.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        system.assertEquals(null, output.get('NOVAL'));
        map<string, object> sample1 = (map<string, object>)output.get('SAMPLE1');
        system.assertNotEquals(null, sample1);
        system.assertEquals('VALUE1', sample1.get('FIELD1'));

        overcast.Globals.IsOCF = true;
        MaterialPortalSite output2 = new MaterialPortalSite();
        output2.loadAsyncResponse('{"output":{"SAMPLE1":{"FIELD1":"VALUE1"}}}');
        overcast.Globals.IsOCF = false;
        system.assertEquals(null, output2.get('NOVAL'));
        map<string, object> sample2 = (map<string, object>)output2.get('SAMPLE1');
        system.assertNotEquals(null, sample2);
        system.assertEquals('VALUE1', sample2.get('FIELD1'));
    }

    @istest
    private static void testGetMetadata() {
        MaterialPortalSite output = new MaterialPortalSite();
        map<string, overcast.TypeInfo> metadata = output.getMetadata();
        
        set<string> keys = metadata.keySet();
        system.assert(keys.contains('ZMM_PORTAL'));
        
        overcast.TypeInfo first = metadata.get(output.getTopTypeName());
        system.assertNotEquals(null, first);
        system.assertEquals('ZMM_PORTAL', first.name);
    }

}